// * imports, libraries and modules */

/** random color generator
 *  1. Intensity ranges 0 - 255
 */

const randomFunction = () => {
    return Math.ceil(Math.random() * (255 - 0) + 0); 
};

const randomColorGenerator = () => {

};

